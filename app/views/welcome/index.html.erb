<div class="container">
  <h1>Welcome to Weather or Not</h1>

  <h2>What would you like to do?</h2>
  <% if @current_user %>

  <%= form_for(:session, url: session_path(session), :method => :delete) do |f| %>
    <%= f.submit :value => "Logout" %>
  <% end %>


<!--   <form method="GET" action="/entries">
    <button class="button" type="submit">View Entries</button>
  </form> -->


  <%= form_for(:entry, url: entries_path, :method => 'GET') do |f| %>
    <%= f.submit :value => "View Entries" %>
  <% end %>

  <% else %>

    <%= form_for :user, url: new_user_path, :html => { :method => 'GET' } do |f| %>
      <%= f.submit "Sign Up" %>
    <% end %>

    <%= form_for :session, url: new_session_path, :html => { :method => 'GET' } do |f| %>
      <%= f.submit "Login" %>
    <% end %>

  <% end %>

</div>